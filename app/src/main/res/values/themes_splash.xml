<resources>
    <style name="Theme.TimeWallet.Splash" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Use a solid background color for the splash theme to avoid drawable resolution issues at startup -->
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
