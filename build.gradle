// Top-level build file for project-wide configuration.
// Module-specific plugins (Android application, Kotlin, KSP) are declared in the module's build.gradle.

// Repository management is controlled in `settings.gradle` via dependencyResolutionManagement.

